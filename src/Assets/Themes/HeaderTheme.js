import { createTheme} from "@mui/material/styles";

const HeaderTheme = createTheme({
    palette: {
        primary: {
            main: '#000000',
        },
        secondary: {
            main: '#ffffff',
        },
    }
});

export default HeaderTheme;