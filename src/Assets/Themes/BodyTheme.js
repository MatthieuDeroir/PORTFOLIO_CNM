import { createTheme} from "@mui/material/styles";

const BodyTheme = createTheme({
    palette: {
        primary: {
            main: '#000000',
        },
        secondary: {
            main: '#ffffff',
        },
    }
});

export default BodyTheme;