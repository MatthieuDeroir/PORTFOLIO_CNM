import { createTheme} from "@mui/material/styles";

const FooterTheme = createTheme({
    palette: {
        primary: {
            main: '#000000',
        },
        secondary: {
            main: '#ffffff',
        },
    }
});

export default FooterTheme;